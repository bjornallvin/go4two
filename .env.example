# Database connection string
# Get from Supabase: Project Settings → Database → Connection string
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# PartyKit host (for production, set to your deployed PartyKit URL)
# Local development uses localhost:1999 by default
NEXT_PUBLIC_PARTYKIT_HOST=localhost:1999

# Game creation security
# Password required to create games (shown to user)
CREATE_GAME_PASSWORD=your-secret-password
# Token stored in cookie after auth (should be a random string)
AUTH_TOKEN=your-random-auth-token
